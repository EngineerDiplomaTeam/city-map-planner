import { FindImageWithIconPipe } from './find-image-with-icon.pipe';

describe('FindImageWithIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FindImageWithIconPipe();
    expect(pipe).toBeTruthy();
  });
});
