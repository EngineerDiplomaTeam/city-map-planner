import { RequireUpdatePipe } from './require-update.pipe';

describe('RequireUpdatePipe', () => {
  it('create an instance', () => {
    const pipe = new RequireUpdatePipe();
    expect(pipe).toBeTruthy();
  });
});
