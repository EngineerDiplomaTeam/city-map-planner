import { IdToColorPipe } from './id-to-color.pipe';

describe('IdToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
