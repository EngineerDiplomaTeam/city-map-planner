import { ToDurationPipe } from './to-duration.pipe';

describe('ToDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
