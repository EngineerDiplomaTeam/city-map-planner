import { ToDataEventPipe } from './to-data-event.pipe';

describe('ToDataEventPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDataEventPipe();
    expect(pipe).toBeTruthy();
  });
});
